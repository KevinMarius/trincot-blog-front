import { Outlet } from "react-router-dom";
import React from "react";

export default function Category() {
    return (
        <React.Fragment>
            <Outlet />
        </React.Fragment>
    )
}